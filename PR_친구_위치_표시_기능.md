# 🗺️ 친구 위치 표시 기능 구현

## 📋 개요

온라인 상태인 친구들의 위치를 지도상에 실시간으로 표시하는 기능을 구현했습니다. 사용자는 친구들의 현재 위치를 확인하고, 프로필 이미지를 마커로 표시하여 직관적으로 친구들의 활동을 파악할 수 있습니다.

## ✨ 주요 기능

### 🎯 핵심 기능
- **실시간 친구 위치 표시**: 30초마다 자동 갱신
- **프로필 이미지 마커**: 친구의 프로필 사진을 원형 마커로 표시
- **친구 정보창**: 마커 클릭 시 상세 정보 표시 (닉네임, 위치 좌표)
- **토글 기능**: 친구 위치 표시 ON/OFF
- **친구 수 표시**: 현재 온라인 친구 수 실시간 표시

### 🎨 UI/UX 개선사항
- **친구 위치 정보 패널**: 하단에 친구 목록 표시 (최대 3명)
- **새로고침 버튼**: 수동으로 친구 위치 갱신 가능
- **로딩 상태**: API 호출 중 로딩 표시
- **빈 상태 처리**: 친구가 없을 때 적절한 메시지 표시
- **기본 프로필 이미지**: SVG 기반 기본 이미지 제공

## 🛠️ 기술적 구현

### 📁 새로 생성된 파일
```
src/
├── types/
│   └── friend.ts                    # 친구 관련 타입 정의
├── utils/
│   └── friendApi.ts                 # 친구 위치 조회 API 함수
├── components/
│   └── FriendLocationMarkers.tsx    # 친구 위치 마커 컴포넌트
└── hooks/
    └── useFriendLocations.ts        # 친구 위치 조회 커스텀 훅
```

### 🔧 수정된 파일
```
src/
├── pages/
│   └── index.tsx                    # 메인 페이지에 친구 위치 기능 통합
└── hooks/
    └── index.tsx                    # 훅 export 추가
```

### 🏗️ 아키텍처

#### 1. 타입 정의 (`src/types/friend.ts`)
```typescript
interface FriendLocation {
  friendId: number;
  memberId: number;
  nickname: string;
  profile: string;
  location: {
    lng: number;
    lat: number;
  };
}
```

#### 2. API 함수 (`src/utils/friendApi.ts`)
- `GET /api/friends/location` 엔드포인트 연동
- 쿠키 기반 인증 토큰 처리
- HTTP 상태 코드별 에러 처리

#### 3. 커스텀 훅 (`src/hooks/useFriendLocations.ts`)
- 30초마다 자동 갱신
- 로딩 상태 및 에러 관리
- 수동 새로고침 기능

#### 4. 마커 컴포넌트 (`src/components/FriendLocationMarkers.tsx`)
- 프로필 이미지 마커 생성
- 클릭 이벤트 처리
- 정보창 표시

## 🎮 사용 방법

### 1. 친구 위치 표시
- 우측 상단의 "친구" 버튼 클릭
- 지도상에 친구들의 위치가 마커로 표시됨

### 2. 친구 정보 확인
- 지도상의 친구 마커 클릭
- 친구의 닉네임, 프로필, 위치 좌표 확인

### 3. 수동 갱신
- 새로고침 버튼으로 즉시 친구 위치 갱신

### 4. 친구 목록 확인
- 하단 패널에서 최근 3명의 친구 목록 확인

## 🔌 API 명세

### 엔드포인트
```
GET /api/friends/location
```

### 요청 헤더
```
Content-Type: application/json
Accept: application/json
Cookie: ACCESS_TOKEN=abc123; REFRESH_TOKEN=def456
```

### 응답 예시
```json
{
  "httpStatus": 200,
  "message": "친구 위치 조회 성공",
  "friends": [
    {
      "friendId": 11,
      "memberId": 21,
      "nickname": "준경",
      "profile": "https://image.example.com",
      "location": {
        "lng": 127.0000,
        "lat": 37.0000
      }
    }
  ]
}
```

### 에러 처리
- **400**: 잘못된 요청 파라미터
- **401**: 인증 토큰 없음/만료
- **403**: 친구 관계가 아님
- **404**: 사용자 정보 없음
- **500**: 서버 오류

## 🧪 테스트 시나리오

### 1. 기본 기능 테스트
- [ ] 친구 위치 토글 버튼 클릭
- [ ] 마커 표시 확인
- [ ] 정보창 클릭 테스트
- [ ] 새로고침 버튼 동작 확인

### 2. API 연동 테스트
- [ ] 실제 백엔드 API 연결
- [ ] 인증 토큰 확인
- [ ] 에러 상황 테스트 (401, 403, 500)

### 3. 성능 테스트
- [ ] 많은 친구가 있을 때 마커 렌더링
- [ ] 자동 갱신 성능 확인
- [ ] 메모리 누수 방지 확인

## 🔒 보안 고려사항

- **인증**: 쿠키 기반 토큰으로 API 호출
- **권한**: 친구 관계가 있는 사용자만 위치 조회 가능
- **개인정보**: 위치 정보는 친구 관계에서만 공유

## 🚀 성능 최적화

- **마커 풀링**: 기존 마커 재사용으로 메모리 효율성 향상
- **자동 갱신**: 30초 간격으로 적절한 갱신 주기 설정
- **이미지 최적화**: SVG 기반 기본 프로필 이미지 사용
- **메모리 관리**: 컴포넌트 언마운트 시 마커 정리

## 📱 반응형 디자인

- **모바일 친화적**: 터치 인터페이스 최적화
- **적응형 레이아웃**: 다양한 화면 크기 지원
- **접근성**: 키보드 네비게이션 지원

## 🔄 향후 개선 계획

- [ ] 친구와의 실시간 채팅 기능
- [ ] 친구 위치 히스토리 추적
- [ ] 친구 그룹별 위치 표시
- [ ] 위치 기반 알림 기능
- [ ] 친구와의 거리 계산 및 표시

## 📝 변경 사항 요약

### 추가된 기능
- ✅ 친구 위치 실시간 표시
- ✅ 프로필 이미지 마커
- ✅ 친구 정보창
- ✅ 자동/수동 갱신
- ✅ 친구 목록 패널

### 개선된 UX
- ✅ 직관적인 토글 버튼
- ✅ 로딩 상태 표시
- ✅ 빈 상태 처리
- ✅ 에러 메시지 개선

### 기술적 개선
- ✅ TypeScript 타입 안전성
- ✅ 컴포넌트 분리 및 재사용성
- ✅ 성능 최적화
- ✅ 메모리 관리

---

**작성자**: [개발자명]  
**작성일**: 2024년 [월] [일]  
**버전**: v1.0.0 
