# 🍪 쿠키 확인 가이드

## 📋 크롬 개발자 도구로 쿠키 확인하는 방법

### 1. **개발자 도구 열기**
- **F12** 키 또는 **우클릭 → 검사**
- 또는 **Cmd + Option + I** (Mac)

### 2. **Application 탭으로 이동**
- **Application** 탭 클릭
- 왼쪽 사이드바에서 **Storage** → **Cookies** 확장
- **http://localhost:5173** 클릭

### 3. **쿠키 목록 확인**
다음 쿠키들이 있는지 확인:

#### ✅ **정상적인 쿠키들**
- **JSESSIONID**: 세션 ID (HttpOnly)
- **ACCESS_TOKEN**: 액세스 토큰 (JavaScript에서 접근 가능)

#### ❌ **문제가 있는 경우**
- 쿠키가 전혀 없음
- **ACCESS_TOKEN** 쿠키가 없음
- 쿠키 도메인이 `localhost:8080`으로 설정됨

### 4. **쿠키 상세 정보 확인**
각 쿠키의 다음 속성들을 확인:

| 속성 | 값 | 설명 |
|------|-----|------|
| Name | ACCESS_TOKEN | 쿠키 이름 |
| Value | eyJ0eXBlIjoiQUNDRVNTX1RPS0VOIiwiYWxnIjoiSFM1MTIifQ... | 토큰 값 |
| Domain | localhost | 도메인 (중요!) |
| Path | / | 경로 |
| Expires | Session | 만료 시간 |
| HttpOnly | ❌ | JavaScript 접근 가능 여부 |
| Secure | ❌ | HTTPS 전용 여부 |
| SameSite | Lax | SameSite 정책 |

### 5. **문제 해결 방법**

#### **문제 1: 쿠키가 없음**
```bash
# OAuth 로그인 다시 수행
http://localhost:8080/oauth2/authorization/kakao
```

#### **문제 2: 도메인이 localhost:8080**
백엔드에서 쿠키 설정 수정 필요:
```java
Cookie accessTokenCookie = new Cookie("ACCESS_TOKEN", token);
accessTokenCookie.setDomain("localhost"); // localhost:8080이 아닌 localhost
accessTokenCookie.setPath("/");
response.addCookie(accessTokenCookie);
```

#### **문제 3: HttpOnly가 true**
JavaScript에서 접근할 수 없으므로 백엔드 수정 필요:
```java
// HttpOnly를 false로 설정하거나 별도 쿠키 생성
```

### 6. **콘솔에서 쿠키 확인**
브라우저 콘솔에서 다음 명령어로 확인:

```javascript
// 모든 쿠키 확인
console.log('모든 쿠키:', document.cookie);

// ACCESS_TOKEN 쿠키만 확인
const cookies = document.cookie.split(';');
const accessTokenCookie = cookies.find(cookie => 
  cookie.trim().startsWith('ACCESS_TOKEN=')
);
console.log('ACCESS_TOKEN:', accessTokenCookie);
```

### 7. **Network 탭에서 쿠키 전송 확인**
1. **Network** 탭 열기
2. API 요청 클릭
3. **Request Headers**에서 `Cookie` 헤더 확인
4. **Response Headers**에서 `Set-Cookie` 헤더 확인

---

**이 가이드를 통해 쿠키 설정 문제를 정확히 파악하고 해결할 수 있습니다!** 🔍 
