# 🎯 산책로 리뷰 수정/삭제 기능 구현

## 📋 개요
산책로 상세보기에서 사용자가 작성한 리뷰를 수정하거나 삭제할 수 있는 기능을 구현했습니다.

## ✨ 주요 기능

### 1. 동적 리뷰 버튼
- **리뷰 미작성**: "리뷰 남기기" 버튼 (주황색) → 리뷰 작성 페이지로 이동
- **리뷰 작성 완료**: "리뷰 수정" 버튼 (파란색) → 리뷰 수정 페이지로 이동
- 로딩 중에는 스피너 표시

### 2. 리뷰 수정 페이지 (`/reviews/edit/:reviewId/:trailId`)
- 기존 별점과 리뷰 내용을 그대로 표시
- 별점 클릭으로 변경 가능 (1~5점)
- 리뷰 내용 수정 (500자 제한)
- 수정 완료 시 이전 페이지로 자동 이동

### 3. 리뷰 삭제 기능
- 우측 상단 "삭제" 버튼
- 확인 다이얼로그 표시
- 삭제 완료 시 이전 페이지로 자동 이동

### 4. API 연동
- **PUT** `/api/trails/reviews/{reviewId}` - 리뷰 수정
- **DELETE** `/api/trails/reviews/{reviewId}` - 리뷰 삭제
- 적절한 에러 처리 및 타임아웃 설정

## 🔧 기술적 구현

### 새로 추가된 파일
- `src/pages/reviews/edit.tsx` - 리뷰 수정 페이지
- `src/utils/reviewApi.ts` - API 함수 추가

### 수정된 파일
- `src/components/TrailDetailCard.tsx` - 동적 버튼 및 내비게이션 추가
- `src/App.tsx` - 리뷰 수정 페이지 라우트 추가

### 주요 컴포넌트 구조
```
TrailDetailCard
├── 리뷰 작성 여부 확인 (getTrailReviews)
├── 동적 버튼 렌더링
│   ├── 리뷰 없음 → "리뷰 남기기" → /reviews/:trailId
│   └── 리뷰 있음 → "리뷰 수정" → /reviews/edit/:reviewId/:trailId
└── 리뷰 목록 모달 (TrailReviewsList)

ReviewEditPage
├── 기존 리뷰 데이터 로드
├── 별점 선택 (인터랙티브)
├── 리뷰 내용 수정
├── 수정 완료 처리
└── 삭제 기능
```

## 🎨 UI/UX 개선사항

### 사용자 경험
- 기존 작성 내용을 그대로 표시하여 수정 편의성 향상
- 별점 클릭으로 직관적인 평점 변경
- 500자 제한으로 적절한 리뷰 길이 유도
- 로딩 상태와 에러 상태의 명확한 표시

### 시각적 개선
- 리뷰 작성 여부에 따른 색상 구분
  - 주황색: 리뷰 남기기 (새로운 액션)
  - 파란색: 리뷰 수정 (기존 데이터 수정)
- 일관된 디자인 시스템 적용
- 반응형 레이아웃 지원

## 🔒 보안 및 검증

### 인증 처리
- 모든 리뷰 관련 페이지는 인증이 필요
- `AuthWrapper`를 통한 보호된 라우트

### 권한 검증
- 본인이 작성한 리뷰만 수정/삭제 가능
- API 레벨에서 권한 검증

### 데이터 검증
- 리뷰 내용 빈 값 검증
- 별점 범위 검증 (1~5점)
- 글자 수 제한 (500자)

## 🧪 테스트 시나리오

### 정상 케이스
1. 리뷰 미작성 상태에서 "리뷰 남기기" 버튼 확인
2. 리뷰 작성 후 "리뷰 수정" 버튼으로 변경 확인
3. 수정 페이지에서 기존 데이터 로드 확인
4. 별점 및 내용 수정 후 저장 확인
5. 삭제 버튼으로 리뷰 삭제 확인

### 에러 케이스
1. 네트워크 오류 시 적절한 에러 메시지 표시
2. 권한 없는 리뷰 수정 시도 시 에러 처리
3. 빈 내용으로 수정 시도 시 검증 메시지 표시

## 📱 반응형 지원
- 모바일 환경에서 최적화된 터치 인터페이스
- 태블릿 및 데스크톱 환경 지원
- 다양한 화면 크기에서 일관된 사용자 경험

## 🚀 성능 최적화
- API 호출 시 10초 타임아웃 설정
- 불필요한 리렌더링 방지
- 로딩 상태를 통한 사용자 피드백 제공

## 🔄 상태 관리
- React hooks를 활용한 로컬 상태 관리
- 비동기 작업의 적절한 상태 처리
- 에러 상태의 명확한 관리

---

## 📝 커밋 히스토리
- `a47b40a` - feat: 산책로 리뷰 수정/삭제 기능 구현
- `72fe848` - feat: 산책로 리뷰 조회 기능 구현

## 🎯 다음 단계
- 리뷰 작성/수정 후 실시간 UI 업데이트
- 리뷰 목록 페이지네이션 구현
- 리뷰 신고 기능 추가 
